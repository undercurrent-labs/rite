# E040 Permission denied

## Failing example

```rite compile_fail
this_name_is_definitely_undefined_xyz
```

(Permission denials are runtime `E040`; use `rite run` without `--allow`.)

## Corrected

```rite exec
! @console.println("use --allow fs:read=./data for file access")
```
