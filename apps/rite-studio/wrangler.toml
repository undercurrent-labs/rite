# Standalone Studio deploy (legacy). Prefer the unified product site:
#   apps/rite-web  →  pnpm site:build && pnpm site:deploy
#
# name kept for reference only; production deploys from apps/rite-web/wrangler.toml
name = "rite-studio-standalone"
compatibility_date = "2024-01-01"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
