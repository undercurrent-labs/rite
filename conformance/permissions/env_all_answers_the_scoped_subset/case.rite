// `@env.all` used to demand the blanket `--allow env` and refuse a scoped grant
// outright, even though the only thing it could have returned was the one variable
// that *was* granted. It now answers the subset. PATH is set everywhere this runs,
// and nothing else is granted, so the key list is exactly one name.
^ keys(! @env.all()?)
