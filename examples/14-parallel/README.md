# 14 Parallel

`parallel(xs, f)` — three 300ms subprocesses run together, not in sequence.

**Browser:** no (`@process`)  
**Run:** `rite run examples/14-parallel/main.rite --allow process --allow clock`
